<Window x:Class="SEP_Age.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SEP_Age"
        mc:Ignorable="d"
        Title="MainWindow" Height="768 " Width="1366">
    
    <Grid Margin="0,10,0,0" x:Name="Base">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="15*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Button x:Name="сбросButton" Content="Сброс" Click="сбросButton_Click" Margin="3"/>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,10,0">
                    <Label Content="Проекты" FontSize="16" FontWeight="Bold"/>
                    <DataGrid x:Name="проектыGrid" Margin="0,32,0,0" MouseDoubleClick="проектыGrid_MouseDoubleClick"/>
                </Grid>

                <Grid Grid.Column="1" Margin="0,0,10,0">
                    <Label Content="Площади" FontSize="16" FontWeight="Bold"/>
                    <DataGrid x:Name="площадиGrid" Margin="0,32,0,0" MouseDoubleClick="площадиGrid_MouseDoubleClick"/>
                </Grid>

                <Grid Grid.Column="2" Margin="0,0,10,0">
                    <Label Content="Профили" FontSize="16" FontWeight="Bold"/>
                    <DataGrid x:Name="профилиGrid" Margin="0,32,0,0" MouseDoubleClick="профилиGrid_MouseDoubleClick"/>
                </Grid>

                <Grid Grid.Column="3" Margin="0,0,10,0">
                    <Label Content="Пункты наблюдения" FontSize="16" FontWeight="Bold"/>
                    <DataGrid x:Name="пунктыНаблюденияGrid" Margin="0,32,0,0"  MouseDoubleClick="пунктыНаблюденияGrid_MouseDoubleClick"/>
                </Grid>
            </Grid>
        </Grid>


        <Grid Grid.Column="1" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Label Content="Измерения" FontSize="16" FontWeight="Bold"/>
                <DataGrid x:Name="измеренияGrid" Margin="0,32,0,0" MouseDoubleClick="измеренияGrid_MouseDoubleClick"/>
            </Grid>
            <Image Grid.Row="1" x:Name="pictureBox" Width="420" Height="360" />
            <DataGrid Grid.Row="1" x:Name="кордыGrid" Margin="0,32,0,0" Visibility="Hidden"/>
            <lvc:CartesianChart Grid.Row="1" x:Name="MeasurementsChart" Series="{Binding SeriesCollection}" />
        </Grid>
    </Grid>
</Window>
